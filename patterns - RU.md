## Roman to Integer (#13)
- Ошибка: сделал сложное условие (val == 1 || val == 10 || val == 100)
- Решение: достаточно просто if (val < val_next)

## Longest Common Prefix (#14)
- Ошибка: result += symbol внутри цикла. присваивалось столько раз, сколько повторяется в слове
- Решение: вынести result += symbol за цикл

## Valid Parentheses (#20)
- когда: проверка пар (скобки, теги, вложенность)
- открывающий → push, закрывающий → сравни с top() → pop()
- ошибка: забыл проверить stack.empty() перед top() — порядок условий важен

## Find the Index (#28)
- когда: поиск подстроки в строке
- ошибка: создал ненужный внутренний цикл, substr уже делает сравнение

## Search Insert Position (#35)
- паттерн: Binary Search
- когда: отсортированный массив + поиск элемента/позиции
- суть: mid = (left + right) / 2, сравни nums[mid] с target
  - меньше → left = mid + 1
  - больше → right = mid - 1
  - равно → return mid
- после while → return left (позиция для вставки)
- ошибка: while (left < right) вместо while (left <= right) — пропускал случай left == right
- ошибка: пересчитывал mid в двух местах вместо одного раза в начале цикла

## Length of Last Word (#58)
- Паттерн: итерация с конца строки
- когда: нужно найти что-то в конце строки
- ошибка: s.size() возвращает unsigned — при пустой строке s.size()-1 переполняется → используй (int)s.size()-1
- ошибка: while без проверки i >= 0 перед s[i] — порядок условий важен

## Plus One (#66)
- Паттерн: итерация с конца + перенос разряда
- когда: арифметика над массивом цифр
- суть: digits[i]++ → если < 10 return → иначе ставь 0, иди дальше
- после цикла: insert(begin(), 1) для случая [9,9,9] → [1,0,0,0]
- узнал: cout не выводит vector напрямую, нужен range-based for

## Add Binary (#67)
- Паттерн: два указателя с конца + перенос разряда
- когда: сложение чисел заданных как строки
- s[i] - '0' → конвертация символа в цифру (иначе получаешь ASCII код)
- sum % 2 → текущий бит, sum / 2 → перенос
- result = digit + result → строим с конца, добавляем в начало
- после цикла: if (carry) result = "1" + result
- ошибка: вложенные циклы — это умножение, не сложение
- ошибка: обращение к a[i] когда i < 0 — всегда проверяй if (i >= 0)

## Merge Sorted Array (#88)
- Паттерн: три указателя с конца
- когда: слияние двух отсортированных массивов
- суть: сравниваем с конца, больший пишем в конец nums1
- если i < 0 → берём из nums2, если j < 0 → берём из nums1
- ошибка: break когда один массив кончился — нужно continue с оставшимся
- ошибка: k-- внутри каждого if вместо одного раза после записи

## Pascal's Triangle (#118)
- Паттерн: построение по предыдущей строке
- структура: vector<vector<int>>
- каждая строка размером i+1, заполнена единицами
- середина: row[j] = result[i-1][j-1] + result[i-1][j]
- внутренний цикл от j=1 до j<i (края не трогаем — они уже 1)
- ошибка: push_back до заполнения середины — пушится старая версия
- ошибка: j < i-1 вместо j < i — последний элемент середины не заполнялся

## Pascal's Triangle II (#119)
- то же что 118, но возвращаем одну строку по индексу
- цикл до i <= rowIndex (не i < rowIndex)
- push_back вне if блока — иначе строки 0 и 1 не добавятся
- return result[rowIndex]

## Best Time to Buy and Sell Stock (#121)
- Паттерн: один проход + отслеживание минимума
- min_price = prices[0], max_profit = 0
- на каждом шаге: обновляй min, потом считай prices[i] - min
- return max(0, max_profit)
- ошибка: else if для min — обновлялся либо max либо min, не оба
- ошибка: max_profit инициализировал не нулём — неверный старт

## Valid Palindrome (#125)
- Паттерн: два указателя + предварительная фильтрация
- isalnum(c) → буква или цифра? tolower(c) → нижний регистр
- сначала фильтруй в новую строку, потом два указателя i и j
- i < j — условие цикла (не i <= j, средний элемент не нужен)
- ошибка: сравнивал s[i] вместо sorted[i]
- ошибка: j++ вместо j-- — указатели должны идти навстречу

## Single Number (#136)
- Паттерн: XOR всех элементов
- a ^ a = 0, a ^ 0 = a → парные числа обнуляются, одиночное остаётся
- один цикл, одна переменная result = 0
- O(n) время, O(1) память

## Excel Sheet Column Title (#168)
- Паттерн: перевод числа в 26-ричную систему (A=1...Z=26)
- i-- перед % 26 — иначе Z (26) даёт лишнюю итерацию
- char = 'A' + i % 26 после i--
- result = char + result — добавляем в начало
- i /= 26 — переход к следующему разряду
- ошибка: без i-- цикл делает лишнюю итерацию когда i кратно 26

## Majority Element (#169)
- Паттерн: подсчёт через unordered_map
- проход 1: map[num]++ — считаем количество каждого числа
- проход 2: pair.first = число, pair.second = количество
- if (pair.second > nums.size() / 2) → нашли
- auto& pair : map — auto уместен когда тип длинный и очевиден из контекста